[build]
  command = "npm run build"
  publish = "dist"

# Client-side routing (React Router): serve index.html for any path that
# isn't a real file, so deep links and page refreshes resolve correctly.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
